[package]
name = "opennow-streamer-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
aes.workspace = true
base64.workspace = true
ctr.workspace = true
crc32fast.workspace = true
getrandom.workspace = true
ghash.workspace = true
hmac.workspace = true
  mio.workspace = true
mtu.workspace = true
opennow-streamer-hid = { path = "../opennow-streamer-hid" }
opennow-streamer-protocol = { path = "../opennow-streamer-protocol" }
serde_json.workspace = true
sha1.workspace = true
socket2.workspace = true
subtle.workspace = true
thiserror.workspace = true

[target.'cfg(windows)'.dependencies]
str0m = { workspace = true, features = ["wincrypto-dimpl"] }
windows-sys = { version = "0.61", features = ["Win32_Networking_WinSock", "Win32_NetworkManagement_IpHelper", "Win32_NetworkManagement_Ndis"] }

[target.'cfg(target_os = "macos")'.dependencies]
# Keep transport crypto architecture-independent. The Apple/Swift provider's
# static archive discovery breaks x86_64 cross-builds on Apple Silicon, while
# WebRTC crypto is not on the VideoToolbox/Metal media hot path.
str0m = { workspace = true, features = ["rust-crypto"] }

[target.'cfg(all(unix, not(target_os = "macos")))'.dependencies]
str0m = { workspace = true, features = ["rust-crypto"] }

[dev-dependencies]
serde_json.workspace = true

[features]
sony-peer-probe = []

[[bin]]
name = "nvst-peer-probe"
path = "src/bin/nvst_peer_probe.rs"
required-features = ["sony-peer-probe"]
